import 'package:flutter/material.dart';

/// Constants for UI elements
class UIConstants {
  /// Default padding for most UI containers
  static const double defaultPadding = 16.0;
  
  /// Small padding for tight spaces
  static const double smallPadding = 8.0;
  
  /// Large padding for section separations
  static const double largePadding = 24.0;
  
  /// Default border radius for containers and cards
  static const double defaultBorderRadius = 8.0;
  
  /// Standard icon size
  static const double iconSize = 24.0;
  
  /// Large icon size for emphasis
  static const double largeIconSize = 36.0;
  
  /// Standard button height
  static const double buttonHeight = 48.0;
  
  /// Standard elevation for cards
  static const double cardElevation = 2.0;
} 