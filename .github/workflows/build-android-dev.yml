name: Build Android with DEV API

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  call-reusable-build:
    uses: ./.github/workflows/build-android.yml
    with:
      ref: ${{ github.event.inputs.ref }}
      build_env: dev
    secrets:
      apiURL: ${{ secrets.API_URL_DEV }}
